# Recovery record

The prior temporary V3.1 worktrees were removed by the execution environment. The immutable V3 checkout at `/projects/affine-bundle-solver-task5-v3` was not changed. This branch was recreated at `770fb9c`; no old results, timings, ledger rows, identity files, or checkpoints were copied into V3.1. Earlier exploratory observations are not used as canonical evidence.
