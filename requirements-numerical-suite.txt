# Exact disposable environment for the numerical-suite reference protocol.
# Keep the shared CI pins synchronized with requirements-ci.txt.
numpy==2.5.3
scipy==1.18.1
mpmath==1.4.1
threadpoolctl==3.6.0

# load_digits is part of the protocol. Pin its resolved runtime closure so an
# ambient installation cannot silently change the evidence inputs.
scikit-learn==1.9.1
joblib==1.6.0
cloudpickle==3.1.2
narwhals==2.26.0
